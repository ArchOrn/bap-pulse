export default defineAppConfig({
  ui: {
    colors: {
      // BAP Pulse brand accent — sage green, mirrored from the Flutter app.
      // The full palette lives in assets/css/main.css under @theme.
      primary: 'sage',
      neutral: 'zinc',
      success: 'emerald',
      warning: 'amber',
      error: 'red',
      info: 'sky'
    }
  }
})
