name: bap_pulse
description: "BAP Pulse — championnat interne du club Bad A Paname."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  google_fonts: ^6.2.1
  flutter_bloc: ^9.1.1
  bloc: ^9.1.1
  equatable: ^2.0.7
  go_router: ^15.1.2
  flutter_svg: ^2.0.10+1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
